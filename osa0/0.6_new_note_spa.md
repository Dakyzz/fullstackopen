    sequenceDiagram
    participant browser
    participant server

    Note over browser: User writes a note and clicks "Save"

    browser->>server: POST /new_note (note content)
    activate server
    Note right of server: Server saves the new note to database
    server-->>browser: New note data / success response
    deactivate server

    Note right of browser: Browser fetches updated notes list
    browser->>server: GET /data.json
    activate server
    server-->>browser: Updated JSON list of notes
    deactivate server

    Note right of browser: Browser updates the note list on the page

